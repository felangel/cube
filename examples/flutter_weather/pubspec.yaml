name: flutter_weather
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cubit:
    path: ../../packages/cubit
  flutter_cubit:
    path: ../../packages/flutter_cubit
  google_fonts: ^1.1.0
  http: ^0.12.1
  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.8.1
  cubit_test:
    path: ../../packages/cubit_test
  json_serializable: ^3.3.0
  matcher: ^0.12.6
  mockito: ^4.0.0

flutter:
  uses-material-design: true
